{
  "name": "cetinfo-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "live-server src --port=3000",
    "build": "node build.js",
    "start": "live-server src --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}